
@{
    ViewBag.Title = "selectoption";
}
@{ if (Session["adminname"] != null)
    {
  


    <body>
        @*

        <div>

            <input type="button" value="Add Products" onclick="@("window.location.href='" + Url.Action("addprd", "Users") + "'");" />
        </div>
        *@
        @*<div>

            <input type="button" value="Check Products" onclick="@("window.location.href='" +Url.Action("showprd","products")+"'");" />
        </div>
        *@
        @*
        <div>

            <input type="button" value="Check Products" onclick="@("window.location.href='" + Url.Action("Index", "delivereds") + "'");" />
        </div>
        *@





        <div class="container">
            <div class="inner">
                <div class="loginbox">
                    <img src="~/Images/loginicon.png" class="avatar">
                    <h1>Admin</h1>








                    <input type="submit" style="margin-top:30px;padding: 10px 10px;" value="ADD PRODCUTS" onclick="@("window.location.href='" + Url.Action("addprd", "Users") + "'");" />







                    <input type="submit" style="margin-top:30px;padding: 10px 10px;" value="CHECK ORDER" onclick="@("window.location.href='" + Url.Action("Index", "delivereds") + "'");"






                    <form method="POST" action="">






                        <input type="submit" style="margin-top:30px;padding: 10px 10px;" value="CHECK PRODUCTS" onclick="@("window.location.href='" + Url.Action("ind", "products") + "'");"



                    </form>

                </div>



            </div>


        </div>
    </body>
    }
    else
    {

        Response.Redirect("~/Users/Loginadmin");

    }
}